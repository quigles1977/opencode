Fast content search using regex patterns.

- Supports full regex: "log.*Error", "function\s+\w+"
- Filter files with include: "*.js", "*.{ts,tsx}"
- Returns matching file paths sorted by modification time
- For match counts, use rg via Bash (NOT grep)
- For open-ended search, use Task tool
