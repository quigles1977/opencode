Exact string replacement in files.

Rules:
- Must Read file first (will error otherwise)
- Preserve exact indentation after line number prefix
- Prefer editing over creating new files
- No emojis unless requested
- oldString must be unique or use replaceAll
- Use replaceAll for renaming variables
