Make multiple edits to a single file efficiently. Prefer over Edit for multiple changes.

Process:
1. Read file first to understand contents
2. Provide: file_path (absolute), edits array (oldString, newString, replaceAll)

Rules:
- Edits apply sequentially in order
- All succeed or none applied (atomic)
- Each edit operates on result of previous edit
- Must match exact whitespace/indentation
- oldString ≠ newString
- Use replaceAll for renaming variables
- Never add emojis unless requested
