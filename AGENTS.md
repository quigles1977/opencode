## Code Style
- Keep in one function unless reusable
- No unnecessary destructuring
- Avoid `else`, `try/catch`, `any`, `let`
- Prefer single-word variable names
- Use Bun APIs (Bun.file())

## Debug
Test: `bun dev` in packages/opencode

## Tools
Always use parallel tool calls when applicable
